query QueryAssignments {
  allCourses {
    _id
    name
    assignmentsConnection {
      nodes {
        _id
        name
        state
        dueAt
        submissionTypes
        submissionsConnection {
          nodes {
            submissionStatus
          }
        }
      }
    }
  }
}
